---
title: "手機APP整合Cloudflare Zero Trust"
published: 2023-03-28
description: "當我們使用Cloudflare Zero Trust的Access為我們網站提供保護後，會發現手機的APP被擋在Zero Trust後面，像是Synology NAS、RocketChat、Mail2000等。為了解決這個問題，本篇將說明如何使用Cloudflare Warp…"
image: "/images/medium/05d408f4b14d82a8.png"
tags: []
category: "Cloudflare"
seriesOrder: 0
draft: false
---
* * *

當我們使用Cloudflare Zero Trust的Access為我們網站提供保護後，會發現手機的APP被擋在Zero Trust後面，像是Synology NAS、RocketChat、Mail2000等。為了解決這個問題，本篇將說明如何使用Cloudflare Warp APP與Cloudflare Gateway，讓我們的手機也通過零信任。

* * *

#### Cloudflare Access

當我們將DNS與CDN交給Cloudflare之後，我們對外的出入口就會是Cloudflare，當網路流量訪問你的服務時候，Cloudflare Access可以要求使用者進行驗證才能訪問。

![](/images/medium/05d408f4b14d82a8.png)

當使用者第一次訪問服務時候，就會跳轉到上面Cloudflare Access登入畫面，只要在其中一個網頁登入過，就會放行其他也需要Access登入的網站。

![](/images/medium/267946f38a434275.png)

在後台就可以看到訪問紀錄

![](/images/medium/0af8a4d4d2485ded.png)

* * *

#### Cloudflare WARP VPN

[Cloudflare WARP](https://1.1.1.1/)是個免費的VPN服務，可以保護使用者網路，使用WARP會將裝置的DNS改使用Cloudflare的1.1.1.1。

![](/images/medium/5869c83ac7f085cb.png)

* * *

#### Cloudflare Gateway

如果我們需要監控使用者流量就會用到Gateway，Gateway可以監控並管理WARP過來的流量，例如可以限制使用者可以訪問的網站、網路目標、DNS。

![](/images/medium/c0897c8ed2188fd9.png)

在後台可以監控使用者的DNS紀錄、網路紀錄、HTTP紀錄。

![](/images/medium/2a313d5fc8a6d594.png)

* * *

#### 設定教學

我們先準備好一個domain已經上好Cloudflare Access，接下來我們要允許Gateway的用戶可以跳過驗證直接訪問。

如果你要監控使用HTTP的記錄的話，就必須在裝置安裝Cloudflare憑證，如果不裝憑證，只能監控到DNS與Network。設備安裝用的憑證可以在`Zero Trust後台 > Settings > Downloads`找到。我們這個範例就以不用安裝憑證為例，所以也就監控不到HTTP流量。

第一步，到`Zero Trust後台 > Settings > Network`找到`Proxy`將他啟用，找到`TLS decryption`將它關掉，如果要監控HTTP流量則要開啟`TLS decryption`並在裝置安裝憑證。

![](/images/medium/8856f7976064e53d.png)

第二步，我們要決定哪些網路流量要過VPN，還是只有自己網站，如果監控所有流量，可能導致部分服務無法使用，需要另外加入白名單，詳見[這篇](https://chouhsiang.medium.com/%E8%A7%A3%E6%B1%BAcloudflare-warp%E7%84%A1%E6%B3%95%E5%8F%96%E5%BE%97ip-e037675204af)。因此我們這次範例只讓我們自己domain的流量走VPN。

我們先找到domain的IP，我的網域是twcanhelp.me，你會發現\*.twcanhelp.me的DNS正解都會是相同的，我們先記下這兩組IP。

![](/images/medium/e7e8e2c64a5a7b59.png)

第三步，到`Zero Trust後台 > Settings > WARP Client`找到`Device settings`中的`Default` Profile，點選右邊 `⋮` ，選擇`Configure`。

![](/images/medium/d7181c7a745a6b85.png)

接著找到Split Tunnels，勾選Include IPs and domains，只讓我們指定網站的流量過VPN，點選Manage，將剛剛上面兩筆IP新增進去，建議不要新增Domain，因為在iPhone上會無效。

第四步，到`Zero Trust後台 > Settings > WARP Client`找到`WARP client checks`，並新增`Gateway`。

![](/images/medium/b31322af96832cd8.png)

第五步，到`Zero Trust後台 > Access > Applications`， 找到你設定的domain，在Policy新增一筆，Action為Service Auth，Include Selector為Gateway，如下圖，再來把Policy排序排到第一個。這樣就完成了

![](/images/medium/d9080c9361e65c1b.png)

* * *

#### 使用者設定

在使用者的裝置先安裝[Cloudflare WARP](https://1.1.1.1/)，進入APP，找到`設定 > 帳戶 > 登入 Cloudflare Zero Trust`，輸入團隊名稱，並登入驗證。再來就開啟VPN，就可以訪問服務了。

* * *

#### 結語

透過整合Cloudflare Gateway，可以解決Access導致APP無法使用的問題。對於電腦不需要另外安裝WARP也看已透過Access訪問網站，只有手機APP用戶需要安裝。
